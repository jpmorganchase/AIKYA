import React from 'react';

import Logo from "../Logo";

const MainNavbar = (props) => (
    <div elevation={0} {...props}>
     
                <Logo />
         
       
    </div>
);

export default MainNavbar;
